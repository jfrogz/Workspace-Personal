package mx.vw.swf.cdi;

import io.datafx.controller.ViewConfiguration;
import io.datafx.controller.flow.Flow;
import io.datafx.controller.flow.FlowHandler;
import io.datafx.controller.flow.context.ViewFlowContext;
import io.datafx.core.ExceptionHandler;

/**
 * Created by dzm152z on 12/02/2015.
 */
public class SWFFlowHandler extends FlowHandler {
    private ViewConfiguration viewConfiguration2;

    public SWFFlowHandler(Flow flow, ViewFlowContext flowContext) {
        super(flow, flowContext);
    }

    public SWFFlowHandler(Flow flow, ViewFlowContext flowContext, ViewConfiguration viewConfiguration) {
        super(flow, flowContext, viewConfiguration);
    }

    public SWFFlowHandler(Flow flow, ViewFlowContext flowContext, ViewConfiguration viewConfiguration, ExceptionHandler exceptionHandler) {
        super(flow, flowContext, viewConfiguration, exceptionHandler);
        viewConfiguration2 = viewConfiguration;
    }
}
